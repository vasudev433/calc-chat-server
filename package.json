{
  "name": "vault-chat-server",
  "version": "1.0.0",
  "description": "Vault Chat WebSocket + WebRTC Signalling Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
